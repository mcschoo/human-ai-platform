services:
  caddy:
    ports: !override
      - "${PUBLIC_BIND_ADDRESS:-0.0.0.0}:80:80"
      - "${PUBLIC_BIND_ADDRESS:-0.0.0.0}:443:443"
      - "127.0.0.1:${ADMIN_PORT:-2019}:2019"
    environment:
      CADDY_SITE_ADDRESS: ${TLS_DOMAIN:?Set TLS_DOMAIN in .env}
